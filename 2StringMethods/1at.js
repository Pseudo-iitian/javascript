let str = 'Abhishek';
let pos = 3;
console.log(str.at(pos));
console.log(`The charachter at ${pos} is ${str.at(pos)}`);

// goes it goes and round and round 
let len = str.length;
let newpos = -1; // -7 -> len - 7 => 8 - 7 = 1 pos at 1 is b
console.log(`the charachter at ${newpos} is ${str.at(newpos)}`);